import { defineConfig } from 'vite';

export default defineConfig({
  // El base debe coincidir con el nombre de tu repositorio en GitHub para que los assets carguen bien
  base: '/resilient-fermi/',
});
